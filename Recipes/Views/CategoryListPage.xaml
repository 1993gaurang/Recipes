<?xml version="1.0" encoding="utf-8" ?>
<base:BaseContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:base="clr-namespace:Recipes.Views.Base"
    x:Class="Recipes.Views.CategoryListPage"
    xmlns:models="clr-namespace:Recipes.Models"
    x:DataType="vm:CategoryListPageViewModel"
    xmlns:vm="clr-namespace:Recipes.ViewModels"
    Title="{Binding SelectedCategoryString}">
    <base:BaseContentPage.BindingContext>
        <vm:CategoryListPageViewModel/>
    </base:BaseContentPage.BindingContext>
    <Grid>
        <CollectionView
            ItemsSource="{Binding RecipeMeals}"
            SelectionMode="Single"
            x:Name="RecipesCollectionView"
            SelectionChangedCommand="{Binding MealClickedCommand}"
            SelectionChangedCommandParameter="{Binding SelectedItem, Source={x:Reference RecipesCollectionView}}">
            <CollectionView.ItemTemplate>
                <DataTemplate
                    x:DataType="models:MealData">
                    <Grid
                        
                        RowDefinitions="Auto"
                        ColumnDefinitions="Auto,*">
                        
                        <Image
                            Source="{Binding StrMealThumb}"
                            Aspect="AspectFill"
                            Margin="20"
                            HeightRequest="60"
                            WidthRequest="60" />
                        <Label
                            Grid.Column="1"
                            
                            VerticalOptions="Center"
                            FontSize="Medium"
                            Text="{Binding StrMeal}"
                            FontAttributes="Bold" />
                        <BoxView
                            Grid.ColumnSpan="2"
                            VerticalOptions="End"
                            BackgroundColor="Gray"
                            HeightRequest="0.5"
                            />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        <ActivityIndicator
            IsVisible="{Binding IsBusy}"
            IsRunning="{Binding IsBusy}"
            HorizontalOptions="Center"
            VerticalOptions="Center"/>
    </Grid>
</base:BaseContentPage>
